version: 2
updates:
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: weekly
    open-pull-requests-limit: 5
    groups:
      # Expo pins exact versions of its own packages; keep them moving together.
      expo:
        patterns:
          - "expo*"
          - "@expo/*"
